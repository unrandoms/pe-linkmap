; Module-definition file for {{ dll_name }} proxy
; Generated by dll-proxy-gen <https://github.com/unrandoms/dll-proxy-gen>
;
; Use this file as an alternative (or complement) to the #pragma forwarding
; stubs in proxy.c.  Link with:
;   MSVC:  link /DLL /DEF:proxy.def /OUT:{{ dll_name }} proxy.obj
;   MinGW: gcc -shared -Wl,--output-def,proxy.def -o {{ dll_name }} proxy.o

LIBRARY {{ dll_name }}

EXPORTS
{% for exp in named_exports %}
    {{ exp.name }} = {{ orig_stem }}.{{ exp.name }} @{{ exp.ordinal }}
{% endfor %}
{% for exp in ordinal_exports %}
    _Ordinal{{ exp.ordinal }} = {{ orig_stem }}.#{{ exp.ordinal }} @{{ exp.ordinal }} NONAME
{% endfor %}
